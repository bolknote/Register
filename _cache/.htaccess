# Cache entries can contain configuration and generated PHP. They are never
# public resources and must not be served even if PHP handling is disabled.
Options -Indexes -ExecCGI

<IfModule mod_authz_core.c>
    Require all denied
</IfModule>

<IfModule !mod_authz_core.c>
    Deny from all
</IfModule>
